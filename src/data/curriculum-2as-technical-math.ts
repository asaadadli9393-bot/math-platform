// ============================================================
//  بيانات المنهاج الرسمي — السنة الثانية شعبة تقني رياضي (2AS-TechnicalMath)
//  إشراف بيداغوجي: الأستاذ عدلي أسعد
//  مرتّب حسب تدرّج وزارة التربية الوطنية 2022
// ============================================================
//  27 أسبوعًا — 162 ساعة (6 ساعات/أسبوع)
//  الفصل 1: الدوال (18h) + الاشتقاقية (15h) + الاحتمالات (12h) + المرجح (9h) + معالجة (6h)
//  الفصل 2: النهايات (15h) + الزوايا الموجهة (12h) + التحولات النقلية (9h) + الجداء السلمي (18h) + معالجة (6h)
//  الفصل 3: المتتاليات (12h) + الهندسة في الفضاء (18h) + معالجة (6h)
// ============================================================

import type { UnitSeed } from "./curriculum";
import { shared2AS_Units } from "./curriculum-2as-scientific-units";

// ============================================================
//  تصدير المنهاج الكامل للسنة الثانية شعبة تقني رياضي
//  نفس وحدات الشعب العلمية الأخرى (Math/Experimental) لكن بترتيب وحجم مختلف
// ============================================================
export const curriculum2ASTechnicalMath: UnitSeed[] = [
  // === الفصل الأول (T1) === 60 ساعة
  shared2AS_Units.Functions,         // 1. الدوال (18h)
  shared2AS_Units.Derivatives,       // 2. الاشتقاقية (15h)
  shared2AS_Units.Probability,       // 3. الاحتمالات (12h)
  shared2AS_Units.Barycenter,        // 4. المرجح (9h)
  // === الفصل الثاني (T2) === 60 ساعة
  shared2AS_Units.Limits,            // 5. النهايات (15h)
  shared2AS_Units.DirectedAngles,    // 6. الزوايا الموجهة (12h)
  shared2AS_Units.Transformations,   // 7. التحولات النقلية (9h)
  shared2AS_Units.ScalarProduct,     // 8. الجداء السلمي (18h)
  // === الفصل الثالث (T3) === 36 ساعة
  shared2AS_Units.Sequences,         // 9. المتتاليات (12h)
  shared2AS_Units.SpaceGeometry,     // 10. الهندسة في الفضاء (18h)
];
