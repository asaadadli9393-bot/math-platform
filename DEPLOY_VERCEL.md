# نشر منصة «تدرّج» على Vercel — دليل خطوة بخطوة

المنصة **جاهزة 100% للنشر**: بناء الإنتاج نجح بلا أخطاء، وملف `vercel.json` مضبوط (المنطقة `fra1` — باريس، الأقرب للجزائر، مع ترويسات أمان وتخزين مؤقت للملفات الثابتة).

اختر **واحدة** من الطرائق الثلاث أدناه.

---

## الطريقة 1: النشر المباشر عبر Vercel CLI (الأسهل والسريع)

1. سجّل الدخول إلى [vercel.com](https://vercel.com) بحسابك (يمكن الدخول بحساب GitHub).
2. أنشئ Token من: **vercel.com/account/settings/tokens** ← زر *Create Token*، انسخه.
3. على حاسوبك (بعد تحميل حزمة المشروع `tadaruj-platform.zip` وفك ضغطها):

```bash
cd tadaruj-platform
npm install          # أو: bun install
npm i -g vercel
vercel login         # أدخل بريدك وأكّد الرمز
vercel --prod        # انشر نسخة الإنتاج مباشرة
```

4. بعد دقيقة تقريباً ستحصل على رابط مثل: `https://tadaruj.vercel.app`

> 💡 يمكنك أيضاً تنفيذ النشر عن طريق المساعد مباشرة: أنشئ Token وأرسله في المحادثة، فيُنفَّذ `vercel --prod --token=...` من بيئة العمل فوراً. (يمكنك حذف الـ Token من حسابك بعد النشر انتهاءً من استعماله).

---

## الطريقة 2: عبر GitHub (الأفضل للاستدامة — نشر تلقائي مع كل تعديل)

1. ارفع المشروع إلى مستودع GitHub جديد (مثل `tadaruj`):

```bash
cd tadaruj-platform
git init
git add .
git commit -m "منصة تدرّج للرياضيات — تحت إشراف الأستاذ عدلي اسعد"
git branch -M main
git remote add origin https://github.com/USERNAME/tadaruj.git
git push -u origin main
```

2. ادخل [vercel.com/new](https://vercel.com/new) ← اختر المستودع `tadaruj` ← زر **Import**.
3. لا تعدّل أي إعداد (Vercel يكتشف Next.js تلقائياً) ← زر **Deploy**.
4. خلال دقيقتين ستكون المنصة منشورة على رابط دائم، وكل `git push` لاحق ينشر التحديث تلقائياً.

---

## الطريقة 3: السحب والإفلات (بدون أوامر)

1. فك ضغط `tadaruj-platform.zip` على حاسوبك.
2. ادخل [vercel.com/new](https://vercel.com/new).
3. إن لم يكن لديك مستودع، استخدم زر **Deploy بدون Git** عبر واجهة Vercel (قسم *Deploy from terminal* لا يلزم — يمكنك أيضاً سحب مجلد المشروع إلى نافذة الطرفية `vercel` بعد تثبيتها كما في الطريقة 1).

---

## ماذا يحدث عند النشر؟

| البند | القيمة |
|---|---|
| أمر البناء | `next build` (يُكتشف تلقائياً) |
| مجلد الإخراج | `.next` (standalone مدعوم) |
| منطقة الاستضافة | `fra1` — باريس (أقل زمن استجابة للجزائر) |
| متغيرات بيئة | **لا حاجة لأي متغير** — كل الوظائف محلية في المتصفح |
| قاعدة بيانات | لا تُستعمل (تقدّم الطلبة يُحفظ في `localStorage` جهازهم) |
| النطاق المجاني | `*.vercel.app` مجاني نهائياً على خطة Hobby |

## ملاحظات مهمة

- **لا حاجة لأي مفاتيح API** — المنصة لا تعتمد على خدمات خارجية.
- تقدّم الطلبة (التمارين المحلولة، نتائج الاختبارات) يُخزَّن محلياً على جهاز كل طالب.
- لإضافة نطاق خاص (مثل `tadaruj-dz.com`) اشتره ثم أضفه من إعدادات المشروع في Vercel ← *Domains*.
- بعد النشر يُستحسن تفعيل *Analytics* من لوحة Vercel لمتابعة عدد الزوار.
